# Entorno local (no versionar)
.env
.env.local
.env.*.local

# Dependencias
node_modules/

# Build / artefactos generados (NestJS y Jest)
dist/
coverage/
*.tsbuildinfo

# Logs / IDE
logs/
*.log
.idea/
.vscode/
*.swp

# Binarios locales de herramientas (p. ej. Cloud SQL Auth Proxy)
.tools/

# OS
.DS_Store
Thumbs.db

# Docker (overrides locales opcionales)
docker-compose.override.yml

# Respaldos locales de base de datos
*.dump

# Respaldos locales Cloud Run / deploy (no versionar)
backend.yaml
frontend.yaml
