import { Module } from '@nestjs/common';

/**
 * Utilidades transversales del backend (enums, base entity, filtros futuros).
 * En Sprint 6 no exporta providers; sirve como ancla arquitectónica.
 */
@Module({})
export class CommonModule {}
